# 참치 위키 검색 층 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code가 참치·수산 도메인 사실을 원자료 대신 먼저 읽는 개체 단위 마크다운 위키 31장과, 그 위키의 출처 규율을 강제하는 훅·인덱스·검색 규칙을 세운다.

**Architecture:** `~/tuna-dashboard/wiki/` 아래 개체(기업 20·RFMO 4·어법 2·인증 2·제도 3)마다 한 장. 프론트매터 + 고정 6절(요약·사실·관계·미확인·출처·정정 이력). `wiki/scripts/article.py`가 문서를 파싱하고, `check_article.py`가 규율을 검사하며, `build_index.py`가 `INDEX.md`를 생성한다. 전역 훅 `verify_wiki.sh`가 저장 시점에 `check_article.py`를 부른다. 검색 규칙은 `wiki/README.md`에 두고 `CLAUDE.md`가 임포트한다.

**Tech Stack:** Python 3 (표준 라이브러리 + pyyaml, pytest 9), bash 훅, Claude Code PostToolUse 훅, git.

**Spec:** `docs/superpowers/specs/2026-09-09-tuna-wiki-design.md`

## Global Constraints

- 작업 저장소: `~/tuna-dashboard` (main 브랜치). **push 금지** — 사용자가 명시 요청할 때까지.
- 위키 루트: `~/tuna-dashboard/wiki/`. 하위 디렉토리는 정확히 `companies/ rfmo/ gear/ certs/ regimes/ scripts/`.
- 문서 파일명: 정식 표기, 공백은 `_`. 위키링크 `[[이름]]`은 파일명 stem과 일치해야 한다.
- 프론트매터 필수 키: `type name last_verified confidence`. `type ∈ {company, rfmo, gear, cert, regime}`, `confidence ∈ {A, B, C}`, `last_verified`는 `YYYY-MM-DD`.
- `## 사실`의 모든 불릿은 `[출처 n]`으로 끝난다. 예외 없음.
- 위키 문서 본문은 한국어. 코드 주석은 영어 허용.
- 통합프로필 원본은 Drive에 그대로 둔다. 복사하지 않는다. 위키 문서는 프로필을 출처 [1]로 가리킨다.
- writer ≠ reviewer. 기업 7장은 반드시 별도 리뷰어가 프로필 원문과 대조한다. 리뷰어 우선순위: `adversarial-reviewer` 서브에이전트 → 실패 시 `bash ~/.claude/harness/lib/route.sh code-verify "<프롬프트>"`.
- 커밋 메시지: 한국어 + 끝에 ` [CC]`, 본문 마지막 줄에 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- 파일 삭제·git history 재작성 금지. 설정 파일(`settings.json`)은 백업 후 수정.
- 서브에이전트가 `organization has disabled Claude subscription access`로 죽으면 같은 방식으로 재시도하지 말고 메인이 직접 하거나 route.sh로 우회한다 (2026-09-08 실측).

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `wiki/README.md` | 검색 규칙 문장 + 구조 설명. `CLAUDE.md`가 임포트 |
| `wiki/_template.md` | 새 문서의 출발점. 프론트매터 + 6절 |
| `wiki/INDEX.md` | 생성물. 손으로 안 쓴다 |
| `wiki/scripts/article.py` | 문서 파싱 한 가지: `parse_article(path) -> Article` |
| `wiki/scripts/check_article.py` | 규율 검사 4종. CLI 종료코드 = 실패 수 |
| `wiki/scripts/build_index.py` | 프론트매터·요약에서 `INDEX.md` 생성 |
| `wiki/scripts/tests/test_article.py` | 파서 테스트 |
| `wiki/scripts/tests/test_check_article.py` | 검사기 테스트 |
| `wiki/scripts/tests/test_build_index.py` | 인덱스 생성 테스트 |
| `~/.claude/harness/verify/verify_wiki.sh` | PostToolUse 훅. stdin JSON → 경로 필터 → `check_article.py` 호출 → block/pass |
| `~/.claude/settings.json` | 훅 배선 (기존 `Write\|Edit` 그룹에 추가) |
| `~/.claude/skills/tunacompany/SKILL.md` | 검색 규칙 삽입(14줄 뒤) + `## 5. wiki-update` 절 추가(파일 끝) |
| `~/tuna-dashboard/CLAUDE.md` | `@wiki/README.md` 한 줄 추가 |
| `wiki/companies/*.md` ×20, `wiki/rfmo/*.md` ×4, `wiki/gear/*.md` ×2, `wiki/certs/*.md` ×2, `wiki/regimes/*.md` ×3` | 문서 31장 |

---

### Task 1: 위키 골격·템플릿·검색 규칙

**Files:**
- Create: `wiki/README.md`
- Create: `wiki/_template.md`
- Create: `wiki/companies/.gitkeep`, `wiki/rfmo/.gitkeep`, `wiki/gear/.gitkeep`, `wiki/certs/.gitkeep`, `wiki/regimes/.gitkeep`, `wiki/scripts/tests/.gitkeep`
- Modify: `CLAUDE.md` (4줄 → 5줄)
- Test: `wiki/scripts/tests/test_scaffold.py`

**Interfaces:**
- Produces: `wiki/_template.md`의 절 이름 6개 — `## 요약` `## 사실` `## 관계` `## 미확인` `## 출처` `## 정정 이력`. 이후 모든 태스크가 이 절 이름을 그대로 쓴다.

- [ ] **Step 1: 골격 테스트 작성**

`wiki/scripts/tests/test_scaffold.py`:

```python
from pathlib import Path

WIKI = Path(__file__).resolve().parents[2]
REPO = WIKI.parent

def test_dirs_exist():
    for d in ("companies", "rfmo", "gear", "certs", "regimes", "scripts"):
        assert (WIKI / d).is_dir(), d

def test_template_has_six_sections():
    t = (WIKI / "_template.md").read_text(encoding="utf-8")
    for h in ("## 요약", "## 사실", "## 관계", "## 미확인", "## 출처", "## 정정 이력"):
        assert h in t, h
    assert t.startswith("---\n")

def test_readme_has_rule_and_claude_imports_it():
    r = (WIKI / "README.md").read_text(encoding="utf-8")
    assert "wiki/INDEX.md" in r and "## 사실" in r and "보고서에 쓰기 전에 위키" in r
    c = (REPO / "CLAUDE.md").read_text(encoding="utf-8")
    assert "@wiki/README.md" in c
```

- [ ] **Step 2: 실패 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_scaffold.py -v`
Expected: FAIL — `wiki/_template.md` 없음 (FileNotFoundError 또는 assert 실패)

- [ ] **Step 3: 디렉토리·템플릿·README 작성**

```bash
cd ~/tuna-dashboard
mkdir -p wiki/companies wiki/rfmo wiki/gear wiki/certs wiki/regimes wiki/scripts/tests
touch wiki/companies/.gitkeep wiki/rfmo/.gitkeep wiki/gear/.gitkeep wiki/certs/.gitkeep wiki/regimes/.gitkeep wiki/scripts/tests/.gitkeep
```

`wiki/_template.md`:

```markdown
---
type: company
name: 개체명
aliases: []
country: KR
last_verified: 2026-09-09
confidence: C
profile:
---
# 개체명

## 요약
5줄 이내. Claude Code가 이것만 읽고 넘어갈 수 있는 밀도로 쓴다.

## 사실
- 주장 하나가 불릿 하나. 반드시 [출처 1]

## 관계
- [[다른_개체]] 관계 설명

## 미확인
- 항목 — 다음에 뚫을 경로

## 출처
[1] 경로 또는 URL (YYYY-MM-DD 확보)

## 정정 이력
- 2026-09-09 초기 작성
```

`wiki/README.md`:

```markdown
# 참치 위키 — Claude Code 검색 층

참치·수산 도메인의 기업·RFMO·어법·인증·제도 사실이 필요하면 `wiki/INDEX.md`를 먼저 읽고
해당 문서의 `## 사실`을 인용한다. 문서가 없거나 항목이 `## 미확인`에 있을 때만 원자료
(Drive `agri_data/01_수산물(Seafood)/tuna`)로 간다. 원자료에서 새 사실을 확인했으면
보고서에 쓰기 전에 위키 문서에 먼저 넣는다.

## 구조

- `INDEX.md` — 생성물. `python3 wiki/scripts/build_index.py`로 다시 만든다. 손으로 고치지 않는다.
- `companies/` `rfmo/` `gear/` `certs/` `regimes/` — 개체 한 장씩. `_template.md`에서 시작한다.
- `## 사실`의 모든 불릿은 `[출처 n]`으로 끝난다. 저장 시 훅이 검사한다.
- `## 관계`는 `[[파일명stem]]` 위키링크로만 쓴다.

## 갱신

`tunacompany` 스킬의 `## 5. wiki-update` 절을 따른다. 자동 갱신은 없다.

## 설계

`docs/superpowers/specs/2026-09-09-tuna-wiki-design.md`
```

`CLAUDE.md`에 한 줄 추가 (기존 4줄 뒤):

```bash
cd ~/tuna-dashboard && printf '@wiki/README.md\n' >> CLAUDE.md && cat CLAUDE.md
```

Expected: 5줄, 마지막이 `@wiki/README.md`.

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_scaffold.py -v`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
cd ~/tuna-dashboard
git add wiki CLAUDE.md
git commit -m "feat(wiki): 참치 위키 골격·템플릿·검색 규칙 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 문서 파서 `article.py`

**Files:**
- Create: `wiki/scripts/article.py`
- Test: `wiki/scripts/tests/test_article.py`

**Interfaces:**
- Produces:
  ```python
  @dataclass
  class Article:
      path: Path
      fm: dict            # 프론트매터. 없으면 {}
      sections: dict      # {"요약": [줄...], "사실": [줄...], ...}  '## ' 헤딩명 → 헤딩 다음부터 다음 '## ' 전까지의 줄(빈 줄 제외)
      body: str           # 프론트매터 제외 전문

  def parse_article(path: Path) -> Article
  ```
  Task 3·4가 이 시그니처를 그대로 쓴다.

- [ ] **Step 1: 파서 테스트 작성**

`wiki/scripts/tests/test_article.py`:

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from article import parse_article

SAMPLE = """---
type: company
name: 동원산업
aliases: [Dongwon Industries, 동원]
last_verified: 2026-09-09
confidence: A
---
# 동원산업

## 요약
한 줄 요약.
둘째 줄.

## 사실
- IATTC 등록 선망선 12척 [출처 2]

## 관계
- [[IATTC]] 등록

## 미확인
- 어획량 — 원양산업 통계연보

## 출처
[1] Drive/프로필.md (2026-08-30)
[2] RFMO등록부/iattc_ActivePS.html

## 정정 이력
- 2026-09-09 초기
"""

def _write(tmp_path, text):
    p = tmp_path / "동원산업.md"
    p.write_text(text, encoding="utf-8")
    return p

def test_frontmatter_parsed(tmp_path):
    a = parse_article(_write(tmp_path, SAMPLE))
    assert a.fm["type"] == "company"
    assert a.fm["name"] == "동원산업"
    assert a.fm["aliases"] == ["Dongwon Industries", "동원"]
    assert a.fm["confidence"] == "A"

def test_sections_split_and_blank_lines_dropped(tmp_path):
    a = parse_article(_write(tmp_path, SAMPLE))
    assert a.sections["요약"] == ["한 줄 요약.", "둘째 줄."]
    assert a.sections["사실"] == ["- IATTC 등록 선망선 12척 [출처 2]"]
    assert a.sections["출처"] == ["[1] Drive/프로필.md (2026-08-30)", "[2] RFMO등록부/iattc_ActivePS.html"]
    assert set(a.sections) == {"요약", "사실", "관계", "미확인", "출처", "정정 이력"}

def test_no_frontmatter_gives_empty_fm(tmp_path):
    a = parse_article(_write(tmp_path, "# 제목\n\n## 요약\nx\n"))
    assert a.fm == {}
    assert a.sections["요약"] == ["x"]
```

- [ ] **Step 2: 실패 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_article.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'article'`

- [ ] **Step 3: 파서 구현**

`wiki/scripts/article.py`:

```python
"""Parse one wiki article: YAML frontmatter + '## ' sections.

Shared by check_article.py and build_index.py. Standard library + pyyaml only.
"""
from __future__ import annotations

import re
from dataclasses import dataclass, field
from pathlib import Path

import yaml

_FM = re.compile(r"\A---\n(.*?)\n---\n", re.S)
_H2 = re.compile(r"^## (.+?)\s*$")


@dataclass
class Article:
    path: Path
    fm: dict = field(default_factory=dict)
    sections: dict = field(default_factory=dict)
    body: str = ""


def parse_article(path: Path) -> Article:
    text = Path(path).read_text(encoding="utf-8")
    fm: dict = {}
    body = text
    m = _FM.match(text)
    if m:
        loaded = yaml.safe_load(m.group(1)) or {}
        fm = loaded if isinstance(loaded, dict) else {}
        body = text[m.end():]

    sections: dict[str, list[str]] = {}
    current: str | None = None
    for line in body.splitlines():
        h = _H2.match(line)
        if h:
            current = h.group(1)
            sections[current] = []
            continue
        if current is not None and line.strip():
            sections[current].append(line.rstrip())
    return Article(path=Path(path), fm=fm, sections=sections, body=body)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_article.py -v`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
cd ~/tuna-dashboard
git add wiki/scripts/article.py wiki/scripts/tests/test_article.py
git commit -m "feat(wiki): 문서 파서 article.py [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 규율 검사기 `check_article.py`

**Files:**
- Create: `wiki/scripts/check_article.py`
- Test: `wiki/scripts/tests/test_check_article.py`

**Interfaces:**
- Consumes: `article.parse_article(path) -> Article` (Task 2)
- Produces:
  ```python
  def check(path: Path, wiki_root: Path) -> tuple[list[str], list[str]]
      # returns (errors, warnings). errors 비면 통과.
  ```
  CLI: `python3 wiki/scripts/check_article.py <file.md> [--wiki-root DIR]` — 종료코드 = len(errors). stdout에 `ERR: …` / `WARN: …` 한 줄씩. Task 5의 훅이 이 CLI를 부른다.

검사 4종 (스펙 §6):
1. 프론트매터 `type name last_verified confidence` 존재 + 값 형식 → 오류
2. `## 사실` 모든 불릿이 `[출처 \d+]`로 끝남 → 오류
3. 본문의 `[출처 n]` 전부가 `## 출처`에 `[n]`으로 정의됨 → 오류
4. `[[링크]]`가 `wiki_root/**/링크.md`로 풀림 → **경고만**

- [ ] **Step 1: 검사기 테스트 작성**

`wiki/scripts/tests/test_check_article.py`:

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from check_article import check

GOOD = """---
type: company
name: 동원산업
last_verified: 2026-09-09
confidence: A
---
# 동원산업

## 요약
요약.

## 사실
- 선망선 12척 [출처 2]
- 본사 서울 [출처 1]

## 관계
- [[IATTC]] 등록

## 미확인
- 없음

## 출처
[1] 프로필.md
[2] 등록부.html

## 정정 이력
- 2026-09-09 초기
"""

def _wiki(tmp_path, article_text, link_targets=()):
    root = tmp_path / "wiki"
    (root / "companies").mkdir(parents=True)
    (root / "rfmo").mkdir()
    for t in link_targets:
        (root / "rfmo" / f"{t}.md").write_text("---\ntype: rfmo\nname: x\nlast_verified: 2026-09-09\nconfidence: A\n---\n## 요약\nx\n", encoding="utf-8")
    p = root / "companies" / "동원산업.md"
    p.write_text(article_text, encoding="utf-8")
    return p, root

def test_good_article_passes(tmp_path):
    p, root = _wiki(tmp_path, GOOD, link_targets=("IATTC",))
    errors, warnings = check(p, root)
    assert errors == []
    assert warnings == []

def test_missing_frontmatter_key_is_error(tmp_path):
    bad = GOOD.replace("confidence: A\n", "")
    p, root = _wiki(tmp_path, bad, link_targets=("IATTC",))
    errors, _ = check(p, root)
    assert any("confidence" in e for e in errors)

def test_bad_type_value_is_error(tmp_path):
    bad = GOOD.replace("type: company", "type: firm")
    p, root = _wiki(tmp_path, bad, link_targets=("IATTC",))
    errors, _ = check(p, root)
    assert any("type" in e for e in errors)

def test_fact_bullet_without_source_is_error(tmp_path):
    bad = GOOD.replace("- 본사 서울 [출처 1]", "- 본사 서울")
    p, root = _wiki(tmp_path, bad, link_targets=("IATTC",))
    errors, _ = check(p, root)
    assert any("본사 서울" in e and "출처" in e for e in errors)

def test_undefined_source_ref_is_error(tmp_path):
    bad = GOOD.replace("[출처 2]", "[출처 9]")
    p, root = _wiki(tmp_path, bad, link_targets=("IATTC",))
    errors, _ = check(p, root)
    assert any("[출처 9]" in e for e in errors)

def test_broken_wikilink_is_warning_not_error(tmp_path):
    p, root = _wiki(tmp_path, GOOD, link_targets=())   # IATTC.md 없음
    errors, warnings = check(p, root)
    assert errors == []
    assert any("IATTC" in w for w in warnings)

def test_cli_exit_code_equals_error_count(tmp_path):
    import subprocess
    bad = GOOD.replace("- 본사 서울 [출처 1]", "- 본사 서울").replace("confidence: A\n", "")
    p, root = _wiki(tmp_path, bad, link_targets=("IATTC",))
    script = Path(__file__).resolve().parents[1] / "check_article.py"
    r = subprocess.run([sys.executable, str(script), str(p), "--wiki-root", str(root)],
                       capture_output=True, text=True)
    assert r.returncode == 2, r.stdout + r.stderr
    assert r.stdout.count("ERR:") == 2
```

- [ ] **Step 2: 실패 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_check_article.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'check_article'`

- [ ] **Step 3: 검사기 구현**

`wiki/scripts/check_article.py`:

```python
#!/usr/bin/env python3
"""Check one wiki article against the four rules in the design spec (§6).

Exit code = number of errors. Warnings never fail the check.
"""
from __future__ import annotations

import argparse
import re
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from article import parse_article  # noqa: E402

TYPES = {"company", "rfmo", "gear", "cert", "regime"}
CONF = {"A", "B", "C"}
DATE = re.compile(r"^\d{4}-\d{2}-\d{2}$")
SRC_TAIL = re.compile(r"\[출처 (\d+)\]\s*$")
SRC_ANY = re.compile(r"\[출처 (\d+)\]")
SRC_DEF = re.compile(r"^\[(\d+)\]\s")
LINK = re.compile(r"\[\[([^\]|#]+)")


def check(path: Path, wiki_root: Path) -> tuple[list[str], list[str]]:
    a = parse_article(path)
    errors: list[str] = []
    warnings: list[str] = []

    # 1. frontmatter
    for key in ("type", "name", "last_verified", "confidence"):
        if key not in a.fm or a.fm[key] in (None, ""):
            errors.append(f"frontmatter '{key}' 없음")
    t = a.fm.get("type")
    if t is not None and t not in TYPES:
        errors.append(f"frontmatter type '{t}' — {sorted(TYPES)} 중 하나여야 함")
    c = a.fm.get("confidence")
    if c is not None and str(c) not in CONF:
        errors.append(f"frontmatter confidence '{c}' — A/B/C 중 하나여야 함")
    d = a.fm.get("last_verified")
    if d is not None and not DATE.match(str(d)):
        errors.append(f"frontmatter last_verified '{d}' — YYYY-MM-DD 형식이어야 함")

    # 2. every fact bullet ends with [출처 n]
    for line in a.sections.get("사실", []):
        if line.startswith("- ") and not SRC_TAIL.search(line):
            errors.append(f"사실 불릿에 [출처 n] 없음: {line[:60]}")

    # 3. every [출처 n] is defined under ## 출처
    defined = {m.group(1) for line in a.sections.get("출처", []) for m in [SRC_DEF.match(line)] if m}
    used = set(SRC_ANY.findall(a.body))
    for n in sorted(used - defined, key=int):
        errors.append(f"[출처 {n}] 이 ## 출처 에 정의되지 않음")

    # 4. wikilinks resolve (warning only)
    stems = {p.stem for p in Path(wiki_root).rglob("*.md")}
    for name in set(LINK.findall(a.body)):
        if name.strip() not in stems:
            warnings.append(f"[[{name}]] 대상 문서 없음")

    return errors, warnings


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("file")
    ap.add_argument("--wiki-root", default=None)
    args = ap.parse_args()
    path = Path(args.file).resolve()
    root = Path(args.wiki_root).resolve() if args.wiki_root else _find_root(path)
    errors, warnings = check(path, root)
    for e in errors:
        print(f"ERR: {e}")
    for w in warnings:
        print(f"WARN: {w}")
    return len(errors)


def _find_root(path: Path) -> Path:
    for p in path.parents:
        if p.name == "wiki":
            return p
    return path.parent


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_check_article.py -v`
Expected: 7 passed

- [ ] **Step 5: 템플릿이 검사기를 통과하는지 확인 (템플릿 자체가 규율 위반이면 안 된다)**

Run: `cd ~/tuna-dashboard && python3 wiki/scripts/check_article.py wiki/_template.md --wiki-root wiki; echo "exit=$?"`
Expected: `WARN: [[다른_개체]] 대상 문서 없음` 한 줄, `exit=0`

- [ ] **Step 6: 커밋**

```bash
cd ~/tuna-dashboard
git add wiki/scripts/check_article.py wiki/scripts/tests/test_check_article.py
git commit -m "feat(wiki): 규율 검사기 check_article.py — 출처 없는 사실 차단 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 인덱스 생성기 `build_index.py`

**Files:**
- Create: `wiki/scripts/build_index.py`
- Test: `wiki/scripts/tests/test_build_index.py`

**Interfaces:**
- Consumes: `article.parse_article` (Task 2)
- Produces:
  ```python
  def build(wiki_root: Path) -> str     # INDEX.md 전문
  ```
  CLI: `python3 wiki/scripts/build_index.py [--wiki-root DIR]` → `wiki_root/INDEX.md` 덮어씀, stdout에 `INDEX: N 문서` 출력.
  표 열 순서(스펙 §4): `이름 | 별칭 | 요약 | last_verified | confidence`. 유형 순서 고정: company→rfmo→gear→cert→regime. 표시명: 기업/RFMO/어법/인증/제도. 유형 안에서는 이름순.

- [ ] **Step 1: 인덱스 테스트 작성**

`wiki/scripts/tests/test_build_index.py`:

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from build_index import build

def _art(root, sub, name, typ, summary, aliases="[]", conf="A"):
    (root / sub).mkdir(parents=True, exist_ok=True)
    (root / sub / f"{name}.md").write_text(
        f"---\ntype: {typ}\nname: {name}\naliases: {aliases}\nlast_verified: 2026-09-09\nconfidence: {conf}\n---\n"
        f"# {name}\n\n## 요약\n{summary}\n둘째 줄은 안 나온다.\n\n## 사실\n- x [출처 1]\n\n## 출처\n[1] a\n",
        encoding="utf-8")

def test_index_groups_by_type_in_fixed_order_and_sorts_by_name(tmp_path):
    root = tmp_path / "wiki"
    _art(root, "rfmo", "IATTC", "rfmo", "동태평양 참치 위원회")
    _art(root, "companies", "사조그룹", "company", "한국 원양 2위", aliases="[Sajo]")
    _art(root, "companies", "동원산업", "company", "한국 원양 1위", aliases="[Dongwon Industries, 동원]", conf="B")
    out = build(root)
    assert out.index("## 기업 (2)") < out.index("## RFMO (1)")
    rows = [l for l in out.splitlines() if l.startswith("| [[")]
    assert rows[0].startswith("| [[동원산업]] | Dongwon Industries, 동원 | 한국 원양 1위 | 2026-09-09 | B |")
    assert rows[1].startswith("| [[사조그룹]] | Sajo | 한국 원양 2위 | 2026-09-09 | A |")
    assert rows[2].startswith("| [[IATTC]] |  | 동태평양 참치 위원회 | 2026-09-09 | A |")

def test_summary_truncated_to_80_chars(tmp_path):
    root = tmp_path / "wiki"
    _art(root, "gear", "선망", "gear", "가" * 120)
    out = build(root)
    row = [l for l in out.splitlines() if l.startswith("| [[선망]]")][0]
    summary = row.split("|")[3].strip()
    assert len(summary) == 81 and summary.endswith("…")

def test_index_and_template_are_skipped(tmp_path):
    root = tmp_path / "wiki"
    _art(root, "gear", "선망", "gear", "요약")
    (root / "INDEX.md").write_text("old", encoding="utf-8")
    (root / "_template.md").write_text("---\ntype: company\nname: 개체명\nlast_verified: 2026-09-09\nconfidence: C\n---\n## 요약\nt\n", encoding="utf-8")
    out = build(root)
    assert "개체명" not in out
    assert "## 어법 (1)" in out

def test_cli_writes_index(tmp_path):
    import subprocess
    root = tmp_path / "wiki"
    _art(root, "certs", "MSC", "cert", "해양관리협의회 인증")
    script = Path(__file__).resolve().parents[1] / "build_index.py"
    r = subprocess.run([sys.executable, str(script), "--wiki-root", str(root)], capture_output=True, text=True)
    assert r.returncode == 0 and "INDEX: 1 문서" in r.stdout
    assert "[[MSC]]" in (root / "INDEX.md").read_text(encoding="utf-8")
```

- [ ] **Step 2: 실패 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/test_build_index.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'build_index'`

- [ ] **Step 3: 생성기 구현**

`wiki/scripts/build_index.py`:

```python
#!/usr/bin/env python3
"""Generate wiki/INDEX.md from article frontmatter and the first line of ## 요약.

Deterministic. Never edit INDEX.md by hand.
"""
from __future__ import annotations

import argparse
import sys
from datetime import date
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from article import parse_article  # noqa: E402

ORDER = [("company", "기업"), ("rfmo", "RFMO"), ("gear", "어법"), ("cert", "인증"), ("regime", "제도")]
SKIP = {"INDEX.md", "README.md", "_template.md"}
MAX_SUMMARY = 80


def _summary(a) -> str:
    lines = a.sections.get("요약", [])
    s = lines[0].strip() if lines else ""
    return s if len(s) <= MAX_SUMMARY else s[:MAX_SUMMARY] + "…"


def build(wiki_root: Path) -> str:
    wiki_root = Path(wiki_root)
    by_type: dict[str, list] = {t: [] for t, _ in ORDER}
    for p in sorted(wiki_root.rglob("*.md")):
        if p.name in SKIP or "scripts" in p.parts:
            continue
        a = parse_article(p)
        t = a.fm.get("type")
        if t in by_type:
            by_type[t].append(a)

    total = sum(len(v) for v in by_type.values())
    out = [
        "# 참치 위키 INDEX",
        "",
        f"생성: {date.today().isoformat()} · 문서 {total}장 · `python3 wiki/scripts/build_index.py`로 재생성. 손으로 고치지 않는다.",
        "",
        "사실이 필요하면 아래 표에서 개체를 찾아 그 문서의 `## 사실`을 읽는다. 없으면 원자료로 간다.",
        "",
    ]
    for t, label in ORDER:
        arts = sorted(by_type[t], key=lambda a: str(a.fm.get("name", a.path.stem)))
        out.append(f"## {label} ({len(arts)})")
        out.append("")
        out.append("| 이름 | 별칭 | 요약 | last_verified | confidence |")
        out.append("|---|---|---|---|---|")
        for a in arts:
            aliases = a.fm.get("aliases") or []
            alias_s = ", ".join(str(x) for x in aliases) if isinstance(aliases, list) else str(aliases)
            out.append(f"| [[{a.path.stem}]] | {alias_s} | {_summary(a)} | {a.fm.get('last_verified','')} | {a.fm.get('confidence','')} |")
        out.append("")
    return "\n".join(out)


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--wiki-root", default=str(Path(__file__).resolve().parents[1]))
    args = ap.parse_args()
    root = Path(args.wiki_root)
    text = build(root)
    (root / "INDEX.md").write_text(text + "\n", encoding="utf-8")
    n = sum(1 for l in text.splitlines() if l.startswith("| [["))
    print(f"INDEX: {n} 문서")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ~/tuna-dashboard && python3 -m pytest wiki/scripts/tests/ -v`
Expected: 전부 passed (scaffold 3 + article 3 + check 7 + index 4 = 17)

- [ ] **Step 5: 커밋**

```bash
cd ~/tuna-dashboard
git add wiki/scripts/build_index.py wiki/scripts/tests/test_build_index.py
git commit -m "feat(wiki): INDEX.md 생성기 build_index.py [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 훅 `verify_wiki.sh` 배선 + 실발화 확인

**Files:**
- Create: `~/.claude/harness/verify/verify_wiki.sh`
- Modify: `~/.claude/settings.json` (`hooks.PostToolUse` 중 `matcher: "Write|Edit"` 그룹에 항목 추가)

**Interfaces:**
- Consumes: `python3 <wiki>/scripts/check_article.py <file>` CLI (Task 3). 종료코드 = 오류 수, stdout `ERR:`/`WARN:` 줄.
- Produces: PostToolUse 훅. 경로가 `/wiki/(companies|rfmo|gear|certs|regimes)/*.md`일 때만 반응. 오류 있으면 `{"decision":"block","reason":…,"systemMessage":…}` 출력. 경고만 있으면 `{"systemMessage":…}`. 무관 파일이면 출력 없음.

- [ ] **Step 1: 훅 스크립트 작성**

`~/.claude/harness/verify/verify_wiki.sh`:

```bash
#!/bin/bash
# verify_wiki.sh — PostToolUse(Write|Edit) hook
# 참치 위키 문서(wiki/{companies,rfmo,gear,certs,regimes}/*.md)가 저장되면
# wiki/scripts/check_article.py 로 규율 4종을 검사한다. 오류면 block.
# 규칙은 코드(check_article.py)에, 강제는 여기에. 스킬 마크다운에 문장으로 두지 않는다.
set -u
export HARNESS_HOOK_INPUT="$(cat)"

/usr/bin/env python3 << 'PY'
import json, os, re, subprocess, sys
from pathlib import Path

try:
    hook = json.loads(os.environ.get("HARNESS_HOOK_INPUT") or "{}")
except Exception:
    sys.exit(0)

ti = hook.get("tool_input") or {}
tr = hook.get("tool_response") or {}
path = tr.get("filePath") or ti.get("file_path") or ""
if not re.search(r"/wiki/(companies|rfmo|gear|certs|regimes)/[^/]+\.md$", path):
    sys.exit(0)
p = Path(path)
if not p.is_file():
    sys.exit(0)

wiki_root = next((q for q in p.parents if q.name == "wiki"), None)
if wiki_root is None:
    sys.exit(0)
checker = wiki_root / "scripts" / "check_article.py"
if not checker.is_file():
    sys.exit(0)

r = subprocess.run([sys.executable, str(checker), str(p), "--wiki-root", str(wiki_root)],
                   capture_output=True, text=True, timeout=30)
errs = [l[5:] for l in r.stdout.splitlines() if l.startswith("ERR: ")]
warns = [l[6:] for l in r.stdout.splitlines() if l.startswith("WARN: ")]

if errs:
    print(json.dumps({
        "decision": "block",
        "reason": f"[verify_wiki] {p.name} 규율 위반 {len(errs)}건 — 고치기 전엔 위키에 못 들어간다:\n- " + "\n- ".join(errs),
        "systemMessage": f"⚠ {p.name}: 위키 규율 {len(errs)}건 위반",
    }, ensure_ascii=False))
elif warns:
    print(json.dumps({"systemMessage": f"{p.name}: 경고 {len(warns)}건 — " + "; ".join(warns)}, ensure_ascii=False))
PY
exit 0
```

```bash
chmod +x ~/.claude/harness/verify/verify_wiki.sh
bash -n ~/.claude/harness/verify/verify_wiki.sh && echo "문법 OK"
```

- [ ] **Step 2: 파이프 테스트 — 통과·무관·위반 3케이스**

```bash
cd ~/tuna-dashboard
mkdir -p /tmp/wk/wiki/companies /tmp/wk/wiki/rfmo
cp -r wiki/scripts /tmp/wk/wiki/
# 통과 케이스
cat > /tmp/wk/wiki/companies/T.md <<'EOF'
---
type: company
name: T
last_verified: 2026-09-09
confidence: A
---
# T

## 요약
t

## 사실
- 사실 하나 [출처 1]

## 관계
- 없음

## 미확인
- 없음

## 출처
[1] x

## 정정 이력
- 2026-09-09 초기
EOF
echo '{"tool_name":"Write","tool_input":{"file_path":"/tmp/wk/wiki/companies/T.md"}}' | bash ~/.claude/harness/verify/verify_wiki.sh; echo "통과 exit=$? (출력 없어야)"
# 무관 케이스
echo '{"tool_name":"Write","tool_input":{"file_path":"/tmp/wk/README.md"}}' | bash ~/.claude/harness/verify/verify_wiki.sh; echo "무관 exit=$? (출력 없어야)"
# 위반 케이스
sed -i '' 's/- 사실 하나 \[출처 1\]/- 사실 하나/' /tmp/wk/wiki/companies/T.md
echo '{"tool_name":"Write","tool_input":{"file_path":"/tmp/wk/wiki/companies/T.md"}}' | bash ~/.claude/harness/verify/verify_wiki.sh; echo "위반 exit=$? (block JSON 나와야)"
rm -rf /tmp/wk
```

Expected: 통과·무관은 출력 없음. 위반은 `{"decision": "block", "reason": "[verify_wiki] T.md 규율 위반 1건 …`.

- [ ] **Step 3: settings.json 배선 (백업 후)**

```bash
cp ~/.claude/settings.json ~/.claude/settings.json.bak.$(date +%Y%m%d)-wiki
python3 - <<'PY'
import json
from pathlib import Path
p = Path.home()/".claude/settings.json"
d = json.loads(p.read_text())
pt = d["hooks"].setdefault("PostToolUse", [])
CMD = "/Users/idong-geon/.claude/harness/verify/verify_wiki.sh"
if not any(h.get("command")==CMD for m in pt for h in m.get("hooks",[])):
    grp = next((m for m in pt if m.get("matcher")=="Write|Edit"), None)
    if grp is None:
        grp = {"matcher":"Write|Edit","hooks":[]}; pt.append(grp)
    grp["hooks"].append({"type":"command","command":CMD,"timeout":45,"statusMessage":"위키 규율 검사"})
    p.write_text(json.dumps(d, ensure_ascii=False, indent=2)+"\n")
    print("등록")
else:
    print("이미 등록")
PY
jq -e '.hooks.PostToolUse[] | select(.matcher=="Write|Edit") | .hooks[] | select(.command | endswith("verify_wiki.sh")) | .command' ~/.claude/settings.json
```

Expected: `"/Users/idong-geon/.claude/harness/verify/verify_wiki.sh"` 출력, jq exit 0.

- [ ] **Step 4: 실발화 확인 — Write 도구로 위반 문서 저장**

Write 도구로 `~/tuna-dashboard/wiki/companies/_hooktest.md`를 아래 내용으로 저장한다 (사실 불릿에 출처 없음):

```markdown
---
type: company
name: 훅테스트
last_verified: 2026-09-09
confidence: C
---
# 훅테스트

## 요약
t

## 사실
- 출처 없는 문장

## 관계
- 없음

## 미확인
- 없음

## 출처
[1] x

## 정정 이력
- 2026-09-09 초기
```

Expected: 도구 결과에 `PostToolUse:Write hook blocking error … [verify_wiki] _hooktest.md 규율 위반 1건 … 사실 불릿에 [출처 n] 없음` 가 붙는다. 안 붙으면 `/hooks`를 한 번 열어 설정을 다시 읽게 한 뒤 재시도한다.

- [ ] **Step 5: 테스트 파일 제거 + 커밋**

```bash
rm ~/tuna-dashboard/wiki/companies/_hooktest.md
cd ~/tuna-dashboard && git status --short   # wiki/ 에 변경 없어야 함
```

훅 스크립트는 `~/.claude` 아래라 이 저장소 커밋 대상이 아니다. 대신 배선 사실을 README에 한 줄 남긴다:

```bash
cd ~/tuna-dashboard
printf '\n## 훅\n\n`~/.claude/harness/verify/verify_wiki.sh` (PostToolUse Write|Edit) 가 저장 시 `scripts/check_article.py`를 돌린다. 오류면 저장이 막힌다.\n' >> wiki/README.md
git add wiki/README.md
git commit -m "docs(wiki): 훅 배선 기록 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 기업 7장 — 통합프로필에서 이관 (writer≠reviewer)

**Files:**
- Create: `wiki/companies/동원산업.md`, `wiki/companies/사조그룹.md`, `wiki/companies/Jealsa.md`, `wiki/companies/StarKist.md`, `wiki/companies/Century_Pacific.md`, `wiki/companies/Nauterra.md`, `wiki/companies/Bolton_Food.md`

**Interfaces:**
- Consumes: 템플릿(Task 1), `check_article.py` CLI(Task 3), 훅(Task 5 — 저장 시 자동).
- Produces: `## 관계`의 위키링크 대상 이름들 — Task 7이 이 이름 그대로 개체 문서를 만든다. 사용할 링크명은 정확히: `[[IATTC]] [[WCPFC]] [[IOTC]] [[ICCAT]] [[선망]] [[연승]] [[MSC]] [[Dolphin_Safe]] [[IUU]] [[FAD]] [[EEZ]]` 와 기업 파일명들.

프로필 경로 (Drive 루트 `~/Library/CloudStorage/GoogleDrive-cutekorea@gmail.com/내 드라이브/agri_data/01_수산물(Seafood)/tuna/00_참치_관련자료/02_참치_가공·유통·기업/`):

| 문서 | 프로필 |
|---|---|
| 동원산업 | `한국/동원산업/03_통합/10_동원산업_통합프로필.md` |
| 사조그룹 | `한국/사조그룹/03_통합/10_사조그룹_통합프로필.md` |
| Jealsa | `스페인/Jealsa/03_통합/10_Jealsa_통합프로필.md` |
| StarKist | `미국/StarKist/03_통합/10_StarKist_통합프로필.md` |
| Century_Pacific | `필리핀/CenturyPacific/03_통합/10_CenturyPacific_통합프로필.md` |
| Nauterra | `스페인/Nauterra/03_통합/10_Nauterra_통합프로필.md` |
| Bolton_Food | `이탈리아/BoltonFood/03_통합/10_BoltonFood_통합프로필.md` |

아래 Step 1~6을 **7개 문서 각각에** 순서대로 반복한다. 한 문서를 끝내고(커밋까지) 다음으로 간다.

- [ ] **Step 1: 프로필 읽기**

Read 도구로 프로필 전문을 읽는다 (164줄 안팎). 특히 `## 0. 읽는 법`(요약 재료), `## 5~9`(선단·인증·경쟁 — 관계 재료), `## 12. ⚠️미확인`(그대로 계승), `## 13. 파일 지도`(출처 [2] 이후 재료).

- [ ] **Step 2: 문서 작성 규칙에 따라 초안 작성**

Write 도구로 `wiki/companies/<이름>.md`를 만든다. 규칙:

- 프론트매터: `type: company`, `name`은 프로필 제목의 회사명(㈜ 제외), `aliases`는 영문명·약칭, `country`는 ISO 2자리, `last_verified`는 프로필 파일의 `## 14. 정정 이력` 마지막 날짜, `confidence: A`(프로필이 1차 출처 대조를 거쳤으므로), `profile`은 위 표의 Drive 절대 경로.
- `## 요약`: `## 0. 읽는 법`의 30초 요약을 **5줄 이내**로. 새 문장을 짓지 말고 프로필 문장을 압축한다.
- `## 사실`: 20~30 불릿. 프로필 §1~§11에서 **수치가 있는 문장** 우선(매출·선박 수·인증 수·지분율·설립연도). 각 불릿 끝에 `[출처 1]`(프로필 자체) 또는 프로필 §13 파일 지도의 개별 파일을 `[출처 2]`, `[출처 3]`…으로 올려 그것을 단다. 프로필에 없는 사실은 넣지 않는다.
- `## 관계`: 위키링크로만. RFMO 등록(`[[IATTC]]` 등), 주력 어법(`[[선망]]`/`[[연승]]`), 인증(`[[MSC]]`/`[[Dolphin_Safe]]`), 경쟁·거래 기업(`[[사조그룹]]` 등 파일명 stem). 각 줄에 관계 설명 한 구절.
- `## 미확인`: 프로필 `## 12`를 **그대로** 옮긴다. 「다음에 뚫을 경로」까지.
- `## 출처`: `[1] <profile 절대 경로> (<정정 이력 마지막 날짜> 확보)`, 이어서 `## 사실`에서 쓴 번호 전부를 §13 파일 지도의 경로로 정의.
- `## 정정 이력`: `- 2026-09-09 초기 이관 (통합프로필 <로마숫자> 기준)`.

저장 시 훅이 돈다. block되면 사유대로 고쳐 다시 저장한다.

- [ ] **Step 3: 검사기로 재확인**

Run: `cd ~/tuna-dashboard && python3 wiki/scripts/check_article.py wiki/companies/<이름>.md; echo "exit=$?"`
Expected: `exit=0`. `WARN: [[IATTC]] 대상 문서 없음` 류 경고는 정상(Task 7에서 채운다).

- [ ] **Step 4: 리뷰어 대조 (writer≠reviewer)**

Agent 도구, `subagent_type="adversarial-reviewer"`, 프롬프트:

```
대상: ~/tuna-dashboard/wiki/companies/<이름>.md
원문: <프로필 절대 경로>

## 사실 의 불릿을 하나씩 원문과 대조하라. 각 불릿에 대해 다음 중 하나로 판정한다:
- 지지됨: 원문의 어느 절·어느 문장이 그 불릿을 지지하는지 한 줄
- 불일치: 수치·주체·시점이 원문과 다르다. 원문 표현을 인용
- 근거 없음: 원문 어디에도 없다
- 출처 번호 오류: 불릿의 [출처 n]이 가리키는 파일이 그 사실과 무관하다

출력은 「불릿 번호 | 판정 | 근거」 표 하나. 문서를 고치지 마라. 문제 없으면 「전부 지지됨」이라고 써라 —
억지 지적은 검증을 오염시킨다.
```

Agent 호출이 구독 차단으로 실패하면 같은 프롬프트를 `bash ~/.claude/harness/lib/route.sh code-verify "<프롬프트>"`로 보낸다.

리뷰 결과에서 「불일치·근거 없음·출처 번호 오류」로 판정된 불릿은 원문대로 고치거나 삭제한다. 고친 뒤 Step 3을 다시 돈다.

- [ ] **Step 5: 커밋 (문서 하나당 하나)**

```bash
cd ~/tuna-dashboard
git add wiki/companies/<이름>.md
git commit -m "feat(wiki): 기업 문서 <이름> — 통합프로필 이관 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 6: 다음 문서로.** 7개가 끝나면 Task 7로.

---

### Task 7: INDEX 생성 + 검색 규칙 + wiki-update 절 — 값어치 발생 지점

**Files:**
- Create: `wiki/INDEX.md` (생성)
- Modify: `~/.claude/skills/tunacompany/SKILL.md` — 14줄 뒤에 검색 규칙 삽입, 파일 끝(493줄 뒤)에 `## 5. wiki-update` 절 추가

**Interfaces:**
- Consumes: `build_index.py` CLI (Task 4), 기업 7장 (Task 6).
- Produces: `wiki/INDEX.md` — 이후 Claude Code가 참치 사실을 찾을 때 첫 진입점. `tunacompany` 스킬의 `wiki-update` 절 — 갱신 절차의 정본.

- [ ] **Step 1: INDEX 생성**

Run: `cd ~/tuna-dashboard && python3 wiki/scripts/build_index.py && head -20 wiki/INDEX.md`
Expected: `INDEX: 7 문서`, `## 기업 (7)` 표에 7행, 나머지 유형은 `(0)`.

- [ ] **Step 2: tunacompany SKILL.md에 검색 규칙 삽입**

`~/.claude/skills/tunacompany/SKILL.md`의 14줄(「한 줄 원칙」 문단 끝) 바로 뒤에 아래 블록을 넣는다:

```markdown

## 위키 먼저 (2026-09-09)

참치·수산 도메인의 기업·RFMO·어법·인증·제도 사실이 필요하면 `~/tuna-dashboard/wiki/INDEX.md`를 먼저 읽고
해당 문서의 `## 사실`을 인용한다. 문서가 없거나 항목이 `## 미확인`에 있을 때만 원자료
(Drive `agri_data/01_수산물(Seafood)/tuna`)로 간다. **원자료에서 새 사실을 확인했으면 보고서에
쓰기 전에 위키 문서에 먼저 넣는다** (`## 5. wiki-update`).
```

```bash
python3 - <<'PY'
from pathlib import Path
p = Path.home()/".claude/skills/tunacompany/SKILL.md"
lines = p.read_text(encoding="utf-8").split("\n")
assert lines[10].startswith("## 한 줄 원칙"), lines[10]
block = ["", "## 위키 먼저 (2026-09-09)", "",
 "참치·수산 도메인의 기업·RFMO·어법·인증·제도 사실이 필요하면 `~/tuna-dashboard/wiki/INDEX.md`를 먼저 읽고",
 "해당 문서의 `## 사실`을 인용한다. 문서가 없거나 항목이 `## 미확인`에 있을 때만 원자료",
 "(Drive `agri_data/01_수산물(Seafood)/tuna`)로 간다. **원자료에서 새 사실을 확인했으면 보고서에",
 "쓰기 전에 위키 문서에 먼저 넣는다** (`## 5. wiki-update`)."]
if "## 위키 먼저" not in p.read_text(encoding="utf-8"):
    lines[14:14] = block
    p.write_text("\n".join(lines), encoding="utf-8")
    print("삽입")
else:
    print("이미 있음")
PY
sed -n '11,24p' ~/.claude/skills/tunacompany/SKILL.md
```

Expected: 「한 줄 원칙」 문단 뒤에 「## 위키 먼저」 절이 보인다.

- [ ] **Step 3: SKILL.md 끝에 `## 5. wiki-update` 절 추가**

```bash
cat >> ~/.claude/skills/tunacompany/SKILL.md <<'EOF'

## 5. wiki-update — 위키 문서 갱신 (수동 트리거, LLM 실행)

호출: 사용자가 `wiki-update <개체> <새 원자료 경로…>`라고 말한다. 자동으로 돌지 않는다.
트리거 셋 — 기업 해부 발행 직후 · Atuna 브리핑에서 개체가 걸릴 때 · 사용자가 낡았다고 볼 때.

절차:
1. `~/tuna-dashboard/wiki/<유형>/<개체>.md`와 새 원자료를 읽는다.
2. 추가할 사실·바뀐 사실을 **diff 형태로 제안만** 한다. 불릿마다 `[출처 n]`을 붙이고 `## 출처`에 새 번호를 정의한다.
   문서를 통째로 다시 쓰지 않는다. 기존 불릿은 원자료가 반박할 때만 고친다.
3. 사용자 승인.
4. 적용한다. `## 정정 이력`에 `- YYYY-MM-DD <무엇을> <왜> [출처 n]` 한 줄, 프론트매터 `last_verified`를 오늘로.
5. 저장 시 훅(`verify_wiki.sh`)이 돈다. block되면 사유대로 고친다.
6. `python3 ~/tuna-dashboard/wiki/scripts/build_index.py`로 INDEX 재생성. 커밋.

금지: 원자료 없이 사실 추가 · `## 사실`에 출처 없는 불릿 · INDEX.md 수동 편집 · 자동 잡에서 이 절차 호출.
EOF
tail -5 ~/.claude/skills/tunacompany/SKILL.md
```

- [ ] **Step 4: 실사용 테스트 (사용자가 새 세션에서 수행)**

새 Claude Code 세션을 `~/tuna-dashboard`에서 열고 묻는다: **「동원산업 IATTC 등록 선박 몇 척이야」**

확인 기준(스펙 §8):
- 도구 호출 기록에 `wiki/INDEX.md` 또는 `wiki/companies/동원산업.md` Read가 **먼저** 나온다.
- Drive `agri_data` 경로 접근이 **없다**.
- 답에 `[출처 n]` 또는 위키 문서 인용이 있다.

셋 중 하나라도 어긋나면 `wiki/README.md`와 SKILL.md의 규칙 문장을 다시 본다. 이 단계는 사람이 판정한다.

- [ ] **Step 5: 커밋**

```bash
cd ~/tuna-dashboard
git add wiki/INDEX.md
git commit -m "feat(wiki): INDEX 생성 — 기업 7장 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

SKILL.md는 `~/.claude` 아래라 이 저장소 밖이다. 변경 사실만 `wiki/README.md`의 「갱신」 절이 이미 가리킨다.

---

### Task 8: 개체 11장 — RFMO·어법·인증·제도

**Files:**
- Create: `wiki/rfmo/IATTC.md` `wiki/rfmo/WCPFC.md` `wiki/rfmo/IOTC.md` `wiki/rfmo/ICCAT.md` `wiki/gear/선망.md` `wiki/gear/연승.md` `wiki/certs/MSC.md` `wiki/certs/Dolphin_Safe.md` `wiki/regimes/IUU.md` `wiki/regimes/FAD.md` `wiki/regimes/EEZ.md`

**Interfaces:**
- Consumes: 기업 7장의 `## 관계` (Task 6) — 어느 기업이 어느 개체에 걸려 있는지. RFMO 원자료 `~/my-project/tuna_whitepaper_원자료_20260806/RFMO등록부/` (예: `iattc_ActivePS.html` 329KB, table 11개).
- Produces: 기업 문서들이 이미 건 `[[링크]]`의 대상. 파일명은 Task 6 Interfaces의 링크명과 **정확히** 일치해야 한다.

아래 Step 1~4를 11개 문서 각각에 반복한다.

- [ ] **Step 1: 재료 수집**

```bash
cd ~/tuna-dashboard
# 이 개체를 참조하는 기업 문서와 그 줄
grep -rn "\[\[<개체>\]\]" wiki/companies/
# RFMO 원자료 목록 (rfmo 유형일 때)
ls ~/my-project/tuna_whitepaper_원자료_20260806/RFMO등록부/ | grep -i <개체소문자>
```

rfmo 유형은 해당 등록부 파일을 Read 도구로 읽는다(HTML은 처음 400줄만 — 헤더와 첫 표면 충분). gear·cert·regime은 기업 문서 `## 사실`·`## 관계`에서 그 개체를 언급한 불릿을 모은다.

- [ ] **Step 2: 문서 작성**

`wiki/<유형>/<개체>.md`, 템플릿 기준. 규칙:

- `type`은 디렉토리와 일치(`rfmo`/`gear`/`cert`/`regime`). `country` 키는 넣지 않는다. `confidence`: 등록부 원문을 직접 읽었으면 `A`, 기업 문서 인용만이면 `B`.
- `## 요약`: 이 개체가 무엇인지 3줄 이내. (예: IATTC — 동태평양 참치 관리 위원회. 선망선 등록부 `Active PS`를 공개한다.)
- `## 사실`: 3~10 불릿. rfmo는 등록부에서 확인되는 것(관할 해역·공개 등록부 종류·기준일)과 「어느 기업이 등록돼 있나」. 기업 문서에서 가져온 불릿은 그 기업 문서를 `[출처 n]`으로 단다 — 경로는 `~/tuna-dashboard/wiki/companies/<이름>.md`.
- `## 관계`: 이 개체에 걸린 기업들 `[[동원산업]] — IATTC 등록 선망선 12척` 형식. 역링크가 그래프의 엣지가 된다.
- `## 미확인`: 등록부 전수 대조를 안 했으면 「등록부 전수 대조 — 다음 경로: <파일명> 표 N 파싱」.
- `## 출처`: 등록부 파일 절대 경로 또는 기업 문서 경로.

- [ ] **Step 3: 검사기**

Run: `cd ~/tuna-dashboard && python3 wiki/scripts/check_article.py wiki/<유형>/<개체>.md; echo "exit=$?"`
Expected: `exit=0`.

- [ ] **Step 4: 커밋 (유형별로 묶어도 된다)**

```bash
cd ~/tuna-dashboard
git add wiki/rfmo wiki/gear wiki/certs wiki/regimes
git commit -m "feat(wiki): 개체 문서 11장 — RFMO 4·어법 2·인증 2·제도 3 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 5: 위키링크 경고가 사라졌는지 전수 확인**

```bash
cd ~/tuna-dashboard
for f in wiki/companies/*.md; do python3 wiki/scripts/check_article.py "$f" | grep WARN && echo "  ← $f"; done; echo "전수 확인 끝"
```

Expected: WARN 출력 없음. 남으면 파일명과 링크명의 불일치다 — 파일명을 링크명에 맞춘다(링크를 고치지 않는다).

---

### Task 9: 기업 13장 뼈대 — evidence 보고서에서

**Files:**
- Create: `wiki/companies/신라교역.md` `Albacora.md` `Bumble_Bee.md` `FCF.md` `Frabelle.md` `Frinsa.md` `ITOCHU.md` `JAIS.md` `Kyokuyo.md` `Nissui.md` `SeaValue.md` `Thai_Union.md` `Umios.md`

**Interfaces:**
- Consumes: `docs/evidence/company-<slug>-2026-0N/보고서.html` (0.8~5.4MB). 신라교역은 `CONTEXT.md` 121~123줄과 대시보드 데이터.
- Produces: 나머지 13장. `## 미확인`이 본문의 절반 이상이어도 정상이다 — 뼈대다.

evidence 슬러그 대응: albacora bumblebee fcf frabelle frinsa itochu jais kyokuyo nissui seavalue thaiunion umios. (`bolton`은 Task 6의 Bolton_Food와 같은 회사라 만들지 않는다.)

아래 Step 1~4를 13개 각각에 반복한다.

- [ ] **Step 1: 보고서 앞부분 텍스트 추출 (HTML 전체를 읽지 않는다)**

```bash
cd ~/tuna-dashboard
python3 - "$(ls -d docs/evidence/company-<slug>-*)/보고서.html" <<'PY'
import sys, re, html
t = open(sys.argv[1], encoding="utf-8", errors="ignore").read()
t = re.sub(r"<script.*?</script>|<style.*?</style>", "", t, flags=re.S)
t = re.sub(r"<[^>]+>", "\n", t)
t = html.unescape(re.sub(r"\n{2,}", "\n", t))
print(t[:4000])
PY
```

처음 4,000자에 제목·요약·목차가 들어 있다. 수치가 필요하면 같은 스크립트의 `t[:4000]`을 `t[4000:12000]`으로 바꿔 한 구간 더 읽는다. 그 이상은 읽지 않는다 — 이 단계는 뼈대다.

신라교역은 대신 `CONTEXT.md` 121~123줄과 `data/` 아래 신라교역 관련 JSON 파일명을 재료로 쓴다:
`ls ~/tuna-dashboard/data | grep -iE 'silla|fleet|선단'`.

- [ ] **Step 2: 뼈대 문서 작성**

규칙:
- `confidence: B` (발행 보고서 = 검증을 거쳤으나 프로필 단계의 재대조는 안 됨). 신라교역은 `C`.
- `## 요약` 3줄. `## 사실` **3불릿 이상**, 전부 `[출처 1]`(= 보고서.html 절대 경로). 보고서에서 읽은 것만.
- `## 관계`: 보고서 요약에 이름이 나온 RFMO·인증·경쟁사만 위키링크.
- `## 미확인` 첫 줄은 고정: `- 통합프로필 미작성 — 다음 경로: tunacompany 스킬로 해부 편 집필 후 wiki-update`.
- `## 출처`: `[1] ~/tuna-dashboard/docs/evidence/company-<slug>-<날짜>/보고서.html (<폴더 날짜> 발행)`.

- [ ] **Step 3: 검사기**

Run: `cd ~/tuna-dashboard && python3 wiki/scripts/check_article.py wiki/companies/<이름>.md; echo "exit=$?"`
Expected: `exit=0`.

- [ ] **Step 4: 커밋 (13장 한 번에)**

```bash
cd ~/tuna-dashboard
git add wiki/companies
git commit -m "feat(wiki): 기업 13장 뼈대 — evidence 보고서 기준 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 10: 완료 조건 검증 + 마무리

**Files:**
- Modify: `wiki/INDEX.md` (재생성)
- Modify: `wiki/README.md` (완료 상태 한 줄)

**Interfaces:**
- Consumes: 전부.
- Produces: 스펙 §8 완료 조건 4개의 판정.

- [ ] **Step 1: 전 파일 일괄 검사 (완료 조건 2)**

```bash
cd ~/tuna-dashboard
fail=0; n=0
for f in wiki/companies/*.md wiki/rfmo/*.md wiki/gear/*.md wiki/certs/*.md wiki/regimes/*.md; do
  n=$((n+1)); python3 wiki/scripts/check_article.py "$f" >/tmp/chk.txt; rc=$?
  if [ $rc -ne 0 ]; then fail=$((fail+1)); echo "FAIL $f"; cat /tmp/chk.txt; fi
done
echo "문서 $n 장, 실패 $fail 건"
```

Expected: `문서 31 장, 실패 0 건`.

- [ ] **Step 2: INDEX 재생성 (완료 조건 1)**

Run: `cd ~/tuna-dashboard && python3 wiki/scripts/build_index.py && grep -c '^| \[\[' wiki/INDEX.md`
Expected: `INDEX: 31 문서`, 표 행 31.

- [ ] **Step 3: 검색 규칙 두 곳 확인 (완료 조건 3)**

```bash
grep -c "보고서에 쓰기 전에 위키" ~/tuna-dashboard/wiki/README.md ~/.claude/skills/tunacompany/SKILL.md
grep -c "@wiki/README.md" ~/tuna-dashboard/CLAUDE.md
```

Expected: 두 파일 각 `1`, CLAUDE.md `1`.

- [ ] **Step 4: 실사용 테스트 결과 기록 (완료 조건 4 — Task 7 Step 4에서 사용자가 수행)**

사용자에게 Task 7 Step 4 결과를 묻고 `wiki/README.md` 끝에 기록한다:

```bash
cd ~/tuna-dashboard
printf '\n## 상태\n\n- 2026-09-09 문서 31장 · 훅 통과 31/31 · 실사용 테스트: <통과|실패 사유>\n' >> wiki/README.md
```

- [ ] **Step 5: 전체 테스트 + 최종 커밋**

```bash
cd ~/tuna-dashboard
python3 -m pytest wiki/scripts/tests/ -q
git add wiki
git commit -m "feat(wiki): 참치 위키 31장 완성 — 검색 층 가동 [CC]

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
git log --oneline -12 | cat
```

Expected: pytest 17 passed. 커밋 로그에 Task 1~10의 커밋이 순서대로 보인다. **push는 하지 않는다.**

---

## 자가 검토 기록

- 스펙 §1 위치·구조 → Task 1. §2 스키마 → Task 1 템플릿 + Task 3 검사. §3 개체 31 → Task 6(7)+8(11)+9(13)=31. §4 INDEX·규칙 → Task 4·7. §5 갱신 흐름 → Task 7 Step 3. §6 훅 → Task 3·5. §7 시드 순서 → Task 6→7→8→9 (스펙 1→2→3→4→5와 동일 순서; 2와 3은 Task 7로 합침). §8 완료 조건 → Task 10.
- 자리표시자 없음. 「<이름>」「<개체>」「<slug>」는 반복 태스크의 변수이며 각 태스크 머리에 실제 값 목록이 있다.
- 시그니처 일관: `parse_article(path) -> Article` (Task 2) = Task 3·4 import. `check(path, wiki_root) -> (errors, warnings)` (Task 3) = Task 5 훅이 CLI로 호출. `build(wiki_root) -> str` (Task 4) = Task 7·10 CLI 호출. 절 이름 6개는 Task 1 템플릿 = Task 2 파서 = Task 3 검사기 = Task 6·8·9 문서.
- 링크명 일관: Task 6이 거는 `[[IATTC]] [[WCPFC]] [[IOTC]] [[ICCAT]] [[선망]] [[연승]] [[MSC]] [[Dolphin_Safe]] [[IUU]] [[FAD]] [[EEZ]]` = Task 8이 만드는 파일명 stem.
