# 디자인 랩 7라운드 브리프 — 하역 히어로 다변화

## 배경 판정

- r6 하역 «항차 지휘형» ★3 → «하단 정렬이 월 기준으로» → 정렬 반영(r6b)해도 무코멘트 ★3.
- 해석: ①«월 기준»은 정렬이 아니라 **월별 그룹핑**이었을 가능성 ②지휘형 카드 나열 자체가
  하역 도메인에 안 맞을 가능성. r7 = 두 해석을 각각 형태로 만들어 판정받는다.
- 선단 지휘형은 ★5 확정 — 하역만 다변화.

## 공통 계약 (r6 승계 + 추가)

1. `components/design-lab/r7/<이름>.tsx`, default export. 실데이터: `fetch('/api/unloading-db')`,
   vessels 필드·연도 보정 로직은 기존 components/design-lab/r6/UnloadingHeroCommand.tsx 를
   참조·재사용 (복붙 허용 — 시안 단계).
2. **부분 기간 vs 완전 기간 비교 금지** (r6 교훈). 상태색: 하역중=액센트, 대기·완료=muted.
   증감 아닌 곳에 delta 토큰 금지.
3. dsc-card idiom·900 숫자·기준일 의무·한글 100%·tsc strict. recharts 고정 width,
   isAnimationActive={false}. 다른 파일 수정 금지.

## 시안 정의

| 파일 | 컨셉 |
| --- | --- |
| UnloadingMonthBoard.tsx | **월별 그룹 보드** — «2026.08» «2026.07» … 월 섹션 헤더(항차 시작 연월, 최신 월 위) 아래 그 달 선박 카드(선박명·상태·실적 MT·진행률·미니 추세). 상단엔 전 선박 합계 KPI 한 줄(누계 MT·하역중 척수·완료 척수)만 — 선택 개념 없음, 스크롤로 훑는 보드 |
| UnloadingVoyageGantt.tsx | **항차 기간 바** — 선박별 가로 바(항차 시작~종료/현재, dateRange 파싱), x축 월 눈금(2025.12~2026.08), 바 안/옆에 실적 MT·진행률, 하역중 바는 액센트+진행 부분 채움. 바 클릭 → 상단 그 선박 누계·일평균·잔여 KPI 전환 (지휘형 상단 유지) |

## 검수 기준

- 5초 안에 «어느 달에 무엇이 내렸는가»(A) / «항차들이 시간축 위 어디에 있는가»(B)가 읽히는가.
- 실데이터·기준일·tsc 0.
