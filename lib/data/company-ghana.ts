import raw from '@/public/data/companies/ghana_v1.json';

/**
 * 가나 테마의 두 캐너리 기업 해부 인테이크 (신라교역 사내 조사보고서 ⅩⅩⅩⅨ, 2026-09).
 *
 * 가나 테마 어항의 참치 캔 공장 둘. Pioneer Food Cannery 는 타이유니언의 다섯 겹
 * 100% 자회사이고, Cosmo Seafoods 는 신라교역이 23.84% 를 가진 관계기업이다.
 * 같은 항구에서 같은 물고기를 캔에 넣는데 누가 주인인지를 적은 문서의 두께가 다르다.
 *
 * ⚠ **「Cosmo 를 대만 회사가 갖는다」 금지** — 업계지 표기이고 DART 전 연도 검색에서
 *    그 회사 이름은 0건이다. 확인되는 것은 23.84% 이고 나머지 76% 는 비어 있다.
 * ⚠ **「매입 비중이 이례적」 금지** — 같은 표의 키리바시 합작사가 여덟 해 내리 사실상
 *    매출 전량(확인된 다섯 해 99.6~100.5%)이다. 이 편이 세는 것은 비중의 크기가 아니라
 *    **열한 해 0원에서 두 해 만에 뒤집힌 전환**이다.
 * ⚠ **「합병했으니 공장도 합쳤다」 금지** — 원문에 없다. 유럽연합 목록은 도시까지만 적고
 *    두 승인번호가 한 부지인지 두 부지인지는 확정되지 않는다. 번호가 둘 다 살아 있다는
 *    사실은 오히려 건물이 둘일 가능성을 남긴다.
 * ⚠ **「합병이 지분을 25.00%→23.84% 로 바꿨다」 금지** — 원문은 두 사건을 인과로 잇지
 *    않고 교환비율도 공시에 없다. 같은 해에 나란히 적힐 뿐이다.
 * ⚠ **「옐로카드가 수출을 막았다」 금지** — 수출 금지가 아니고, 2021년 재지정 뒤에도
 *    가나 캔참치 수출은 26,567 t(2022) → 31,956 t(2025) 로 늘었다.
 * ⚠ **「MSC 인증을 이 공장들이 받았다」 금지** — 증서가 가리키는 것은 조업 단위이고
 *    공장은 적격 법인 명단에 있을 뿐이다. 원료를 다루려면 별도 관리연쇄 인증이 필요하다.
 * ⚠ **「선박 담보 이자율 6.00%」 금지** — 6.00% 는 대여금 약정이자율이고 담보 조건이
 *    아니다. 담보 주석은 선명도 이자율도 적지 않는다.
 * ⚠ **「846억이 가나 수출의 3분의 1」을 이 공장 몫으로 금지** — 환율은 조회 시점 근사이고
 *    무역 통계는 나라 단위라 테마의 여러 캐너리 가운데 하나로 귀속할 근거가 없다.
 * ⚠ **「두 공장이 다르니 한쪽이 불투명하다」 금지** — 차이는 규모나 의지가 아니라
 *    **모회사가 어느 나라 어느 양식으로 공시하느냐**다. 100% 자회사는 연결에 들어가
 *    나라별 인원까지 집계되고 23.84% 관계기업은 요약재무 아홉 칸으로 끝난다.
 * ⚠ **「외줄낚시 20척」 금지** — 증서에 적힌 것은 2척이다.
 */

const data = raw as unknown as {
  _meta: { 회사: string; 국가: string; 업종: string; 종목: string; 출처: string; 조사일: string };
  card: { numeral: string; tagline: string };
  stats: Record<string, number | string>;
  sourcenotes: string[];
};

/** 숫자 칸만 골라 쓴다 — `PFC_사슬`·`항구`·`옐로카드_2차` 는 문자열이다. */
function n(key: string): number {
  const v = data.stats[key];
  if (typeof v !== 'number') throw new Error(`ghana stats.${key} 가 숫자가 아니다`);
  return v;
}

export const ghanaMeta = data._meta;
export const ghanaCard = data.card;
export const ghanaStats = data.stats;
export const ghanaSourceNotes = data.sourcenotes;

/**
 * 같은 항구의 두 공장에서 소유가 문서로 몇 겹까지 적히는가.
 *
 * ⚠ **비어 있는 76% 를 업계지의 통념으로 메우지 마라.** 그 자리를 매체가 메운 결과가
 *    「대만 회사의 공장」이라는 서술이고 공시 근거는 0건이다.
 * ⚠ 다섯 겹과 한 줄의 차이를 **투명성 의지의 차이로 읽지 마라** — 공시 양식이 정한다.
 */
export function ownershipDepth(): {
  적힌쪽: { 이름: string; 겹수: number; 지분_pct: number; 사슬: string; 미상_pct: number };
  빈쪽: { 이름: string; 겹수: number; 지분_pct: number; 미상_pct: number; 근거: string };
  단서: string;
} {
  return {
    적힌쪽: {
      이름: 'Pioneer Food Cannery(타이유니언)',
      겹수: n('PFC_사슬_층수'),
      지분_pct: n('PFC_지분_pct'),
      사슬: String(data.stats.PFC_사슬),
      미상_pct: n('PFC_소수지분_pct'),
    },
    빈쪽: {
      이름: 'Cosmo Seafoods(신라교역)',
      겹수: 1,
      지분_pct: n('Cosmo_지분_pct'),
      미상_pct: n('Cosmo_미상_지분_pct'),
      근거: '신라교역 관계기업 주석 한 줄이 전부다 — 나머지 주주를 적은 문서가 한국 공시에도 인증기관 데이터베이스에도 없다',
    },
    단서: '두께의 차이는 규모나 의지가 아니라 모회사가 어느 나라 어느 양식으로 공시하느냐에서 온다',
  };
}

/**
 * 열한 해 0원에서 두 해 만에 뒤집힌 매입, 그리고 같은 기간 움직이지 않은 장부.
 *
 * ⚠ **「비중이 이례적」으로 쓰지 마라.** 같은 표의 키리바시 합작사가 여덟 해 내리
 *    사실상 매출 전량이다 — 세는 것은 비중의 크기가 아니라 전환이다.
 * ⚠ **「매입이 늘었으니 회사가 나아졌다」도 아니다.** 매입이 38배가 된 두 해 동안
 *    이 공장의 자본은 오히려 10억 더 벌어졌다.
 */
export function purchaseTurn(): {
  공백_해수: number; 공백_기간: string;
  FY2023_백만원: number; FY2024_백만원: number; FY2025_백만원: number;
  FY2025_비중_pct: number; 배수: number;
  장부금액_백만원: number; 장부금액_경과연수: number; 자본_백만원: number;
  단서: string;
} {
  const f23 = n('매입_2023_백만원');
  const f25 = n('매입_2025_백만원');
  return {
    공백_해수: n('매입_0원_해수'),
    공백_기간: String(data.stats.매입_0원_기간),
    FY2023_백만원: f23,
    FY2024_백만원: n('매입_2024_백만원'),
    FY2025_백만원: f25,
    FY2025_비중_pct: n('매입비중_2025_pct'),
    배수: Math.round(f25 / f23),
    장부금액_백만원: n('Cosmo_장부금액_백만원'),
    장부금액_경과연수: n('Cosmo_장부금액_0_경과연수'),
    자본_백만원: n('Cosmo_자본_2025_백만원'),
    단서: '지분율은 23.84% 에서 한 자리도 움직이지 않았고 장부금액은 아홉 해째 0이다 — 움직인 것은 물건을 사는 금액뿐이다',
  };
}
