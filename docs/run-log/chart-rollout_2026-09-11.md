# 차트·표 고도화 전면 적용 (2026-09-11)

브랜치 `feat/chart-token-pilot` (worktree `~/tuna-wt-chart-pilot`, base main 7756fad0). /pork 시범을 사용자가 승인("마음에 듭니다")한 뒤 전 메뉴로 넓힌다.

1. **완료 조건** — (a) 22개 메뉴(DASHBOARD_PANEL_ORDER)에서 차트 카드가 특별한 경우를 빼면 1행 2개(UI_RULES §4 `repeat(2, 1fr)`) — `measure_layout.js` 로 폭 비율 > 0.6 인 카드가 예외 목록에만 남음 · (b) 단일 시리즈 무지개 막대 0건, 같은 엔티티를 쓰는 차트 묶음은 이름 고정색 + 범례 · (c) `fix_chart_tokens.py --check` 가 live 차트 파일에서 exit 0 · (d) tsc·eslint·vitest 통과 · (e) 픽셀 비교에서 의도한 곳만 바뀜
2. **분해** — ① 조사(Python 도달성 + 브라우저 폭 측정) → ② 레이아웃 2열 전환 → ③ 색 패턴 B·C → ④ 토큰화(A, codemod) → ⑤ 전후 스크린샷
3. **배치** — 조사·편집은 메인이 한다(판단 편집이라 완료 판정이 기계적이지 않다). 반증 검증은 **Grok**(사용자 지시, 작성자 ≠ 검증자)
4. **검수 지점** — 조사 결과 확정 직후 · 커밋/배포 직전(요청이 있을 때만)

## 조사 결과

- 도달 가능한 차트 파일 46개, 표 파일 52개
- 색: 토큰으로 바꿔도 두 테마 모두 화면이 안 바뀌는 hex 는 **9개뿐**. 108개는 라이트 테마가 재정의하는 토큰과 값이 같아서 바꾸면 라이트 화면이 바뀐다. 397개는 맞는 토큰이 없다 → 전면 토큰화는 팔레트 결정이 먼저다
- 무지개 순위 막대(`COLORS[i]`): BeefWidgets(/beef 단독 라우트) 2곳. 대부분은 이미 HIGHLIGHT/BASE·colorForCountry 방식이다

## 진행 기록

### 2026-09-11 — 2열 배치 (사용자 지시 「특별한 경우를 제외하고 그래프는 1열에 2개씩」)

측정: `e2e/measure-chart-layout.js`(`VW=1309 node e2e/measure-chart-layout.js out.json [menus]`) — 19개 메뉴 × 상위 탭을 1440px 로 열고 차트마다 폭(cw)·선 점 수(pts)·막대 범주 수(cats) 기록.
변경 전 전체 폭(cw>0.6) 차트는 약 50개, 나머지는 이미 반폭이었다.

**예외(전체 폭 유지)** — 점·범주 24개 초과(방콕 원어 시세 281주·하역 286주, 파노피 38주 2종, 코스모 36주 2종,
항구별 고시가 103개월, 오징어 선박별 29척·주요 어종 35년, 고등어 30년 2종, 명태 39년·44개월, 참치 1970~2024),
표·지도·일정표(입출항 타임라인), 집계 입도 토글로 24개를 넘을 수 있는 차트(방콕 시세 범위, 트레이더별 반입),
카드 안에서 이미 차트 두 장이 나란한 복합 위젯(가공 공장 가동 현황), 기존 테스트가 고정한 예외(새우 양식과 자연산 75년).

**반폭으로 바꾼 곳**
- 산업 해설 7개 페이지: 근거 차트 17개의 `span: 'full'` 제거, 근거가 한 장뿐이면 쓰던 catchStack 대신 2열 격자 반폭(CommodityIndustryDashboard)
- 코스모: `.g2` 를 auto-fit → `repeat(2, minmax(0, 1fr))`(넓은 화면에서 3~4장으로 벌어지던 것), 카드 5개 span 2 제거
- 파노피: 매출과 손익·가나 보고 vs 상대국 보고 12→6, 역내 입항 물량·척수 3→6, 방콕 기준가 4→6(짝 채널 정책 8→6), 월말 잔액 8→6(짝 4→6)
- 방콕: 재고 점유·트레이더 구성·하역 계절성 12→6
- 부산항(port-intel): 업종별 체류일을 2열 격자 안으로
- 하역 역사 실적(막대 5개)·VDS 4개년 추이: 900px 이상에서 반폭

- 기업 해부(27사): 전체 폭 70장 중 60장은 표라 유지. 차트 6장(프린사 3·알바코라 3, 데이터 3~16개)만 반폭.
  브라우저 측정은 회사 화면에서 차트를 못 잡아(recharts 0) 코드에서 렌더러 이름과 JSON 길이로 판정했다.

**Grok 반증**
- 시범 수정 6건: PASS(6건 모두 재현 입력으로 닫힘 확인)
- 레이아웃: FAIL(3) → 3건 모두 반영 — 코스모 「월별 원어 처리량」(카드 안 6열 표가 반폭에서 가로 스크롤) 전체 폭 복구,
  파노피 역내 입항 물량·척수(38주) 3→12, 골뱅이 「국내 생산 통계」(1990~2025 36점) 전체 폭 복구

**검증** tsc 0 · eslint 0 errors(경고 10건은 CompanyAnatomyDashboard 기존 미사용 변수) · vitest 181 files / 1490 passed ·
화면: 부산항·코스모 생산·파노피 개관·방콕 캐너리·오징어 근거 뷰포트 캡처로 반폭 배치와 차트 렌더 확인.
커밋·배포 안 함.

### 2026-09-11 — 배포 후 라이브 확인에서 나온 후속 (PR #1021 배포 뒤)

라이브(사용자 브라우저 창 1309px)에서 부산항 격자가 한 줄로 접혀 있었다. 1440px 로만 쟀던 탓이다.
- 부산항: `repeat(auto-fit, minmax(min(480px,100%),1fr))` 는 본문 971px 에서 트랙 2개(480×2+20)가 안 들어가 1열로 접힌다
  → `minmax(max(min(100%, 420px), calc(50% - 10px)), 1fr)` — 넓어도 최대 2열, 좁은 화면만 1열.
- 선단 DB: 표가 든 카드(운영사×RFMO 매트릭스)의 최소폭이 표 폭으로 잡혀 짝 차트가 줄바꿈 → 카드 공통 스타일에 `minWidth: 0`.
- 1309px 재측정(13메뉴): 전체 폭 남은 18장은 전부 예외 목록(24개 초과·토글·카드 속 표·복합 위젯)이다.
